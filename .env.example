# Postgres connection string from Neon, Vercel Postgres, Supabase, or another Postgres provider.
# Example shape:
# DATABASE_URL="postgres://user:password@host:5432/database?sslmode=require"
DATABASE_URL=""

# Random secret used later to sign/hash edit tokens.
# Generate with: node -e "console.log(crypto.randomBytes(32).toString('hex'))"
TOKEN_SECRET=""
